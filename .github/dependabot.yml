version: 2
updates:
  - package-ecosystem: npm
    directory: "/"
    schedule:
      interval: weekly
    # Security alerts still appear in the Security tab.
    # This avoids a stream of version-bump pull requests.
    open-pull-requests-limit: 0
